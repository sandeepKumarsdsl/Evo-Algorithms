#ev_plot
"""
import utils

for graph in ['f01', 'f02', 'f06', 'f08', 'f10']:
    utils.plot_experiments('continuous', [
        '.'.join(['default', graph]),
        '.'.join(['diff-threesome', graph]),
        '.'.join(['diff-threesome-small-F', graph]),
        '.'.join(['diff-threesome-small-CF', graph]),
        '.'.join(['diff-basic-adaptive', graph]),
        '.'.join(['diff-threesome-adaptive', graph]),
        '.'.join(['diff-gangbang-adaptive', graph]),
    ])
"""
    
import utils


#for graph in ['f01', 'f02', 'f06', 'f08', 'f10']:
'''
utils.plot_experiments('continuous', [
    '.'.join(['DifferentialEvolution_normal','f01']),
    '.'.join(['DifferentialEvo_normal','f01'])
    
])

utils.plot_experiments('continuous', [
    
    '.'.join(['lamarkism_small_lr_version','f02'])
    
    
])    
#diff-threesome-adaptive-boosted

'.'.join(['lamarkism_high_lr_version','f10']),
'.'.join(['baldwinism_version','f10']),
'.'.join(['DifferentialEvo_Two_Diff','f10'])

utils.plot_experiments('continuous', [
    
    '.'.join(['Baldwin','f01']),
    '.'.join(['Baldwin','f02']),
    '.'.join(['Baldwin','f06']),
    '.'.join(['Baldwin','f08']),
    '.'.join(['Baldwin','f10'])
    
    
]) 

utils.plot_experiments('continuous', [
    
    '.'.join(['Lamarck','f10']),
    '.'.join(['DifferentialEvo_Two_Diff','f10']),
    '.'.join(['Baldwin','f10'])
    
    
    
]) 

utils.plot_experiments('multi', [
    
    '.'.join(['default','ZDT6']),
    '.'.join(['ssv_adapt_mut','ZDT6'])    
    
    
]) 
'''
utils.plot_experiments('tsp', [
    
    '.'.join(['default']),
    '.'.join(['PMX_cross_inverse_mut']),
    '.'.join(['PMX_crossover_swap_mut'])    
    
    
]) 
